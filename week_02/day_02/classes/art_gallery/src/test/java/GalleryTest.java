import static org.assertj.core.api.AssertionsForClassTypes.assertThat;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class GalleryTest {

    /* Gallery gallery;
    Artwork river;

    @BeforeEach
    public void setUp(){
        gallery = new Gallery ("Tate");
        //river = new Artwork ("River", "Mary", 1);
    }

    @Test
    public void canGetArtwork(){
        assertThat(gallery.getArtwork()).isEqualTo(0);
    }

    @Test
    public void canAddArtworkToGallery(){
        gallery.addArtwork(river);
        assertThat(gallery.getArtwork().size()).isEqualTo(1);
    } */
}

